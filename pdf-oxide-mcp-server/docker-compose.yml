version: '3.8'

services:
  pdf-oxide-mcp:
    build: .
    container_name: pdf-oxide-mcp
    ports:
      - "8004:8004"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    command: python server.py --transport streamable-http --port 8004